pcm.dmixed {
    type dmix
    ipc_key 1024
    slave {
        pcm "hw:0,0"  # Replace hw:0,0 with your actual sound card if different
        rate 48000
        channels 2
        period_time 100
    }
}

pcm.duplex {
    type asym
    playback.pcm "dmixed"
    capture.pcm "dsnooped"
}

pcm.!default {
    type plug
    slave.pcm "dmixed"
}
